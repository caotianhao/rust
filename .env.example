# Database (required)
DATABASE_URL=mysql://user:password@127.0.0.1:3306/testr

# Optional: HTTP bind address (default 127.0.0.1:8080)
# APP_BIND=127.0.0.1:8080

# Optional: log level (default info): trace, debug, info, warn, error
# RUST_LOG=info

# --- Solana (optional; omit to disable Solana endpoints) ---
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# SOLANA_RPC_TIMEOUT_SECS=30
# SOLANA_COMMITMENT=confirmed
# Optional: keypair path for fee-payer (read-only endpoints work without it)
# SOLANA_KEYPAIR_PATH=/path/to/keypair.json
